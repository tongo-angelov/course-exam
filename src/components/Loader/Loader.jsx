import { Typography } from "@mui/material";

const Loader = () => {
  return (
    <Typography textAlign="center" sx={{ padding: "20px" }} variant="h5">
      Loading...
    </Typography>
  );
};

export default Loader;
